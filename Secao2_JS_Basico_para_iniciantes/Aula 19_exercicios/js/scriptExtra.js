function formaDePrevinirEventoDeAtualizar()
{
     /*-------------------------------------------------------------
Esta é uma forma de previnir que a pagina atualize ao clicar no botao
**porem nao é a mais usada atualmente

-------------------------------------------------------------------
    form.onsubmit  = function(evento){
//captura o evento submit da classe form
//recebe(=) uma funcao evento!

        evento.preventDefault(); 
//previne o navegador de atualizar por seu evento configurado padrao
        alert(1);
        console.log('Foi enviado!');
        };
-----------------------------------------------------------------*/

}